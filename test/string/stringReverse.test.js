import stringReverse from '../../code/string/stringReverse'

test("stringReverse:Let's take LeetCode contest", () => {
  expect(stringReverse("Let's take LeetCode contest")).toBe(
    "s'teL ekat edoCteeL tsetnoc"
  )
})
